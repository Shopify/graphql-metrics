source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in graphql_metrics.gemspec
gemspec

gem 'graphql', '1.9.14', path: '../../rmosolgo/graphql-ruby'

group :deployment do
  gem 'rake'
end
