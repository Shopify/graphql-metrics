source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "graphql", github: "rmosolgo/graphql-ruby", ref: "better-trace-api"
# Specify your gem's dependencies in graphql_metrics.gemspec
gemspec

group :deployment do
  gem 'rake'
end
